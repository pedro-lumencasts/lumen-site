/** @type {import('next').NextConfig} */
const nextConfig = {
  // No remote image hosts: every frame on the site is a local placeholder from
  // lib/placeholder.ts. Add remotePatterns back when real footage arrives.
};
export default nextConfig;
